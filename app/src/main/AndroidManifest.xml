<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Storage Permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- PiP & Foreground Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Wake Lock for playback -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Vibrate for pattern lock -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Post Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
    android:maxSdkVersion="32" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
    android:maxSdkVersion="29" />
    <application
        android:name=".VidViewerApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VidViewer"
        android:requestLegacyExternalStorage="false"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">


        <!-- Disable Firebase auto-init: app uses only AdMob, not Firebase services.
             play-services-ads 22+ transitively includes firebase-core whose
             FirebaseInitProvider ContentProvider runs before Application.onCreate().
             These flags prevent it crashing with placeholder google-services.json. -->
        <meta-data
            android:name="firebase_auto_init_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

        <!-- AdMob App ID (test ID - replace with real ID for production) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <!-- Splash / Main Entry -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.VidViewer.Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Player Activity -->
        <activity
            android:name=".activities.PlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboard|keyboardHidden"
            android:launchMode="singleTop"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:screenOrientation="sensor" />

        <!-- Folder Browser -->
        <activity
            android:name=".activities.FolderActivity"
            android:exported="false" />

        <!-- Video Details -->
        <activity
            android:name=".activities.VideoDetailsActivity"
            android:exported="false" />

        <!-- Vault Activity -->
        <activity
            android:name=".activities.VaultActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Lock Setup Activity -->
        <activity
            android:name=".activities.LockSetupActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Playlist Activity -->
        <activity
            android:name=".activities.PlaylistActivity"
            android:exported="false" />

        <!-- Settings Activity -->
        <activity
            android:name=".activities.SettingsActivity"
            android:exported="false" />

        <!-- Legal Pages -->
        <activity
            android:name=".activities.PrivacyPolicyActivity"
            android:exported="false" />

        <activity
            android:name=".activities.TermsActivity"
            android:exported="false" />

        <activity
            android:name=".activities.AboutActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ContactActivity"
            android:exported="false" />

        <activity
            android:name=".activities.DisclaimerActivity"
            android:exported="false" />

        <!-- Search Activity -->
        <activity
            android:name=".activities.SearchActivity"
            android:exported="false"
            android:windowSoftInputMode="stateVisible|adjustResize" />

        <!-- Playback Service -->
        <service
            android:name=".services.PlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Boot Receiver -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- File Provider for sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>

</manifest>
